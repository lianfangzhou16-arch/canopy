# 🚀 Neura 指挥中心：保姆级运行指南

欢迎使用百万年薪级别的 Web3 量化指挥中心！本指南将用“大白话”教你如何快速上手。

## 1. 怎么运行？ (大白话版)

### 第一步：准备环境
确保你的电脑安装了 Python。然后在本项目目录下打开终端（命令行），运行：
```bash
pip install -r requirements.txt
```

### 第二步：填入私钥（只有 1 个号时）
1. 看到文件夹里的 `.env.example` 了吗？把它改名为 `.env`。
2. 用记事本打开 `.env`，在 `PRIVATE_KEY=` 后面填入你的私钥。
3. 运行检查：`python tools/wallet_check.py`

### 第三步：启动程序
```bash
python main.py --mode production
```

---

## 2. 多个钱包 + IP 代理怎么搞？

如果你有多个号，并且想给每个号分配不同的 IP 代理，请看这里：

### 第一步：创建钱包配置文件
1. 进入 `configs` 文件夹。
2. 将 `wallets.json.example` 改名为 `wallets.json`。
3. 按照下面的格式填入你的私钥和代理：

```json
[
  {
    "private_key": "你的私钥1",
    "proxy": "http://用户名:密码@IP地址:端口",
    "note": "我的1号机"
  },
  {
    "private_key": "你的私钥2",
    "proxy": "http://用户名:密码@IP地址:端口",
    "note": "我的2号机"
  }
]
```

### 什么是 IP 代理？
*   **格式**：通常是 `http://username:password@1.2.3.4:8080`。
*   **作用**：让每个钱包看起来是从不同的地方登录的，防止被项目方封号。
*   **注意**：如果不填 `proxy` 这一项，程序就会直接用你电脑本机的网络。

---

## 3. 常见问题 (FAQ)

*   **Q: 私钥安不安全？**
    *   A: 私钥只保存在你本地的 `.env` 或 `wallets.json` 里，绝对不要把这两个文件发给别人！
*   **Q: 报错说余额不足？**
    *   A: 运行 `python tools/wallet_check.py`。它会自动计算你当前的钱够不够付手续费（ANKR 币）。
*   **Q: 怎么退出程序？**
    *   A: 在键盘上按 `Ctrl + C`，程序会触发“完美退出”机制，安全关闭。

---

## 🛡️ Web3 铁律提醒
*   **余额预检**：没钱绝对不尝试发交易，省得浪费手续费或报错。
*   **环境隔离**：配置文件不要上传到 GitHub！
*   **并发控制**：我们内置了限制（Semaphore），就算你有 100 个号，也不会因为请求太快被 RPC 封禁。
